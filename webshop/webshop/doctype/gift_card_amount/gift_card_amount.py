# //// Neoffice — added file (no upstream equivalent). Child table of the face values a
# //// shop offers for its gift cards (3bc2d836f1, 2025-02-11).
import frappe
from frappe.model.document import Document

class GiftCardAmount(Document):
    pass
