# //// Neoffice — added file (no upstream equivalent). Child table of Website
# //// Item listing the additional multi-warehouse sources this item displays
# //// when its warehouse_sources_mode is "Custom".

from frappe.model.document import Document


class WebsiteItemWarehouseSource(Document):
	pass
